package com.dentalclinic.dentalclinic_api.config;

import org.springframework.context.annotation.Configuration;

/**
 * Swagger/OpenAPI configuration for dental clinic API documentation
 * Temporarily disabled due to Java 24 compatibility issues
 */
@Configuration
public class SwaggerConfig {

    // Configuration temporarily disabled - SpringDoc will use default settings
    // This allows the application to run while we resolve dependency issues

}
